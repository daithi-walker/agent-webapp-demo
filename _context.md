## Goal
Add a web frontend HTML page for the validator (form + result display), add in-memory validation history (last 50 entries), add GET /history endpoint returning recent validations, add CHANGELOG.md and VERSION file

## This task
Create two new files: (1) VERSION — a plain-text file containing the version string '1.1.0' (bumped from implied 1.0.0 for the new feature additions). (2) CHANGELOG.md — a Keep a Changelog-format file with an [Unreleased] section and a [1.1.0] section dated 2026-05-16 documenting: added in-memory validation history (last 50 entries), added GET /history endpoint, added history display section to HTML frontend.